---
description: "监控 Android APP 的 syscall 行为并生成 AI 分析报告。用法: /svcmon <包名> [--preset re_basic|re_full] [--duration 15s]"
---

Invoke the `tool-svcmon` skill with the provided arguments. The arguments after `/svcmon` are the package name and optional flags.

If no arguments provided, ask the user for the package name.
